import { RemoveCategoryPrefixPipe } from './remove-category-prefix.pipe';

describe('RemoveCategoryPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveCategoryPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
